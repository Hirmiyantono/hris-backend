# Application
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=hris_dev
DB_PASSWORD=your_database_password
DB_DATABASE=hris_dev
DB_SSL=false
DB_POOL_SIZE=10

# Redis Configuration (Will be configured in Task 1.4)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# JWT Configuration (Will be configured in Authentication module)
# JWT_SECRET=your-secret-key
# JWT_EXPIRATION=30m

# Email Configuration (Will be configured in Notification module)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASSWORD=password
